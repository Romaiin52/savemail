<h4>Catégories</h4>
<ul class="nav nav-pills nav-stacked">
	{% for categorie in liste_categories %}
		<li class="dropdown">
			<a class="dropdown-toggle" data-toggle="dropdown" href="#" >
				<span class="label" style="background-color:{{ categorie.color }}">
					{{ render(controller("rmEmailBundle:Default:countEmailsByCat", { 'id': categorie.id } )) }}
				</span>
				{{ categorie.title }}
				<b class="caret"></b>
			</a>
			<ul class="dropdown-menu">
				<li><a href="{{ path('rm_email_home', {'categorieId': categorie.id} ) }}"> <i class="icon-th-list"></i> Voir les emails</a></li>
				<li class="divider"></li>
				<li><a href="{{ path('rm_email_categorie_update', {'id': categorie.id} ) }}"> <i class="icon-cog"></i> Modifier</a></li>
				<li><a href="{{ path('rm_email_categorie_delete', {'id': categorie.id} ) }}"> <i class="icon-remove"></i> Supprimer</a></li>
			</ul>
			
		</li>
	{% endfor %}
</ul>

